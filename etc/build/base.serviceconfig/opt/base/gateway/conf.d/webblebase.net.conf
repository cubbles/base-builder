server {
    listen 80;
    server_name  webblebase.net;
    include /opt/base/gateway/conf.d/webblebase.net.ssl;
    set_by_lua_block $auth_secret {return os.getenv("BASE_AUTH_SECRET")}
    include /opt/base/gateway/conf.d/base.locations;
}